# Port targets — `assert-cmd-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `assert_cmd` that codex-rs uses, and therefore the minimum subset that `assert-cmd-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-13] (rank `241`, tier `direct`, status `To Do`)

**Cargo package name**: `assert_cmd`
**Rust module name (in `use` statements)**: `assert_cmd`

## Summary

- **2** distinct symbols imported by codex-rs
- **6** total `use` statements
- **6** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `assert_cmd::Command`

- **4 `use` site(s)** across **3 codex member crate(s)**: `codex-apply-patch`, `codex-cli`, `codex-core`
- Aliases used: `as AssertCommand`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `apply-patch/tests/suite/cli.rs:1` (`codex-apply-patch`) — `use assert_cmd::Command`
- `apply-patch/tests/suite/tool.rs:1` (`codex-apply-patch`) — `use assert_cmd::Command`
- `cli/tests/execpolicy.rs:2` (`codex-cli`) — `use assert_cmd::Command`
- `core/tests/suite/cli_stream.rs:1` (`codex-core`) — `use assert_cmd::Command as AssertCommand`

### `assert_cmd::prelude::*`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-exec`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/live_cli.rs:2` (`codex-core`) — `use assert_cmd::prelude::*`
- `exec/tests/suite/apply_patch.rs:4` (`codex-exec`) — `use assert_cmd::prelude::*`
